<!DOCTYPE html>
<html lang="es">
  <head>
    <% ssr_head = inertia_ssr_head %>
    <meta charset="utf-8">
    <%# SEO + OpenGraph meta, server-rendered (meta-tags gem): crawlers don't run JS. The
        OpenGraph, Twitter and canonical tags always come from here, with absolute URLs; the
        title and description too, unless SSR produced the page's own <Head> with them. %>
    <%= display_meta_tags(ssr_head.present? ? default_meta_tags.except(:title, :description) : default_meta_tags) %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# The icon (public/, from scripts/brand/favicon.sh): the ICO for tabs, the SVG where it is
        taken, the touch icon for home screens. %>
    <%# Versioned URLs let browsers replace the long-lived public icon assets after a brand refresh. %>
    <link rel="icon" href="/favicon.ico?v=squircle-2026" sizes="48x48">
    <link rel="icon" href="/icon.svg?v=squircle-2026" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png?v=squircle-2026">

    <%# The landing's type: Unbounded 800 uppercase for display, Syne for body, Space Mono for labels. %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@800&family=Syne:wght@400;600&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">

    <%# The real Chile relief (app/frontend/terrain): index + overview fetched here so they
        download alongside the JS. The hrefs follow app/frontend/terrain/terrain-url.ts. %>
    <% Array(@terrain_preloads).each do |path| %>
      <link rel="preload" as="fetch" crossorigin href="<%= path %>">
    <% end %>

    <%= vite_react_refresh_tag %>
    <%= vite_client_tag %>
    <%= vite_typescript_tag "inertia" %>
    <%= ssr_head %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
