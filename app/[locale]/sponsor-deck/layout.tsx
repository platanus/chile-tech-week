import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Platanus Hack 2025 - Sponsor Deck',
};

export default function SponsorDeckLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
